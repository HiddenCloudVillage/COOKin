import React from 'react';
import { Marker } from '@react-google-maps/api';

function EachStore({ eachloc }) {
  return (
    <div>
      <h1>{eachloc}</h1>
    </div>
  );
}

export default EachStore;
